interface:
  display_name: "New RFC"
  short_description: "Draft and revise RFCs with agx workflow conventions"
  default_prompt: "Use $new-rfc to create or revise an RFC that follows this repository's RFC template, metadata fields, and review gates."
